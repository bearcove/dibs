@schema {id crate:squiggles-config@1, cli squiggles}

enabled false
captain true

//workspace {
//    // filter "not package(my-app-queries) and not package(dibs-qgen) and not binary(postgres_integration) and not (test(solver::proptests))"
//    filter "not binary(postgres_integration) and not (test(solver::proptests))"
//}

packages {
    dibs-db-schema {}
}
